#include<stdio.h>
#include<string.h>
typedef struct Student{
	 char name[20];
  int age;
  int average;
 }Student;
 int main(){
 	Student S[100];
 	int n,i,found=0;
 	char x[20];
 	printf("give n:\n");
 	scanf("%d",&n);
 	for(i=0;i<n;i++){
 		printf("give name student:\n");
 		scanf("%s",S[i].name);
 		printf("give age student:\n");
 		scanf("%d",&S[i].age);
 		printf("give average student:\n");
 		scanf("%d",&S[i].average);
		}
		printf("give name student:\n");
		scanf("%s",x);
		for(i=0;i<n;i++){
			if(strcmp(x, S[i].name)==0){
				printf("name student:%s\n",S[i].name);
				printf("age student:%d\n",S[i].age);
				printf("average student:%d\n",S[i].average);
				found=1;
			}
		}
		if(found==0){
			printf("student is not present");
		}
	}
