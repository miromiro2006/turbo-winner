#include<stdio.h>
#include<stdlib.h>
int main(){
	int n,*p,i;
	printf("give n:");
	scanf("%d",&n);
	p=malloc(n*sizeof(int));
	for(i=0;i<n;i++){
		printf("give A[%d]:",i);
		scanf("%d",&*(p+i));
	}
	for(i=0;i<n;i++){
		printf("give A[%d]:%d",i,*(p+i));
	}
	free(p);
}
